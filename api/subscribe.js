import { redis } from './_store.js'; export default async function handler(req,res){ if(req.method!=='POST') return res.status(405).json({error:'Use POST'}); const sub = req.body || {}; if(!sub || !sub.endpoint) return res.status(400).json({error:'Invalid subscription'}); await redis.sadd('subs', JSON.stringify(sub)); res.status(200).json({ok:true}); }